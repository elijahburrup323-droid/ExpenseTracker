<div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 py-8"
     data-controller="account-type-masters"
     data-account-type-masters-api-url-value="<%= api_account_type_masters_path %>"
     data-account-type-masters-csrf-token-value="<%= form_authenticity_token %>">

  <%# Page Header (sticky) %>
  <div class="sticky top-14 z-20 bg-gray-50 dark:bg-gray-900 -mx-4 sm:-mx-6 lg:-mx-8 px-4 sm:px-6 lg:px-8 py-4 mb-2 border-b border-gray-200 dark:border-gray-700 shadow-sm">
    <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3">
    <div>
      <h1 class="text-2xl font-bold text-gray-900 dark:text-white">Account Types Master</h1>
      <p class="mt-1 text-sm text-gray-500 dark:text-gray-400">Manage global account type options available to all users</p>
    </div>
    <div class="flex items-center space-x-2">
      <button type="button"
              class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-lg shadow-sm text-white bg-green-600 hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-500 transition disabled:opacity-50 disabled:cursor-not-allowed"
              data-action="click->account-type-masters#openAddModal">
        <svg class="h-4 w-4 mr-1.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M12 4v16m8-8H4"/>
        </svg>
        Add Account Type
      </button>
    </div>
    </div>
  </div>

  <%# Table %>
  <div class="bg-white dark:bg-gray-800 shadow-sm rounded-lg overflow-hidden ring-1 ring-gray-200 dark:ring-gray-700">
    <table class="min-w-full divide-y divide-gray-200 dark:divide-gray-700">
      <thead class="bg-gray-50 dark:bg-gray-800">
        <tr>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider">Name</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider">Description</th>
          <th scope="col" class="px-6 py-3 text-center text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider w-20">Active</th>
          <th scope="col" class="px-6 py-3 text-center text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider">Actions</th>
        </tr>
      </thead>
      <tbody class="bg-white dark:bg-gray-800 divide-y divide-gray-200 dark:divide-gray-700" data-account-type-masters-target="tableBody">
        <tr>
          <td colspan="4" class="px-6 py-8 text-center text-sm text-gray-400 dark:text-gray-500">Loading...</td>
        </tr>
      </tbody>
    </table>
  </div>

  <%# Info Box %>
  <div class="mt-6 rounded-lg bg-blue-50 dark:bg-blue-900/30 p-4 ring-1 ring-blue-200 dark:ring-blue-800">
    <div class="flex">
      <svg class="h-5 w-5 text-blue-400 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
        <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"/>
      </svg>
      <p class="ml-3 text-sm text-blue-700 dark:text-blue-300">
        These are global account types shared by all users. Use the Active toggle to show/hide a type from user selection.
        Account types in use by any user cannot be deleted&mdash;set them to inactive instead.
      </p>
    </div>
  </div>

  <%# Add/Edit Modal %>
  <div class="hidden fixed inset-0 z-50 overflow-y-auto" data-account-type-masters-target="modal">
    <div class="flex min-h-full items-center justify-center p-4">
      <div class="fixed inset-0 bg-black/50 transition-opacity" data-action="click->account-type-masters#closeModal"></div>
      <div class="relative bg-white dark:bg-gray-800 rounded-xl shadow-2xl w-full max-w-md p-6 ring-1 ring-gray-200 dark:ring-gray-700">
        <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-4" data-account-type-masters-target="modalTitle">Add Account Type</h3>
        <div class="space-y-4">
          <div>
            <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">Display Name</label>
            <input type="text" data-account-type-masters-target="modalName"
                   class="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-700 text-gray-900 dark:text-white focus:ring-2 focus:ring-brand-500 focus:border-brand-500"
                   placeholder="e.g. Checking, Savings"
                   data-action="keydown->account-type-masters#modalKeydown">
          </div>
          <div>
            <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">Description <span class="text-gray-400">(optional)</span></label>
            <input type="text" data-account-type-masters-target="modalDescription"
                   class="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-700 text-gray-900 dark:text-white focus:ring-2 focus:ring-brand-500 focus:border-brand-500"
                   placeholder="Short description"
                   data-action="keydown->account-type-masters#modalKeydown">
          </div>
          <div data-account-type-masters-target="modalError" class="hidden text-sm text-red-600 dark:text-red-400"></div>
        </div>
        <div class="flex justify-end space-x-3 mt-6">
          <button type="button" class="px-4 py-2 text-sm font-medium text-gray-700 dark:text-gray-300 bg-gray-100 dark:bg-gray-700 hover:bg-gray-200 dark:hover:bg-gray-600 rounded-lg transition"
                  data-action="click->account-type-masters#closeModal">Cancel</button>
          <button type="button" class="px-4 py-2 text-sm font-medium text-white bg-brand-600 hover:bg-brand-700 rounded-lg transition"
                  data-action="click->account-type-masters#saveModal">Save</button>
        </div>
      </div>
    </div>
  </div>

  <%# Delete Confirmation Modal %>
  <div class="hidden fixed inset-0 z-50 overflow-y-auto" data-account-type-masters-target="deleteModal">
    <div class="flex min-h-full items-center justify-center p-4">
      <div class="fixed inset-0 bg-black/50 transition-opacity" data-action="click->account-type-masters#closeDeleteModal"></div>
      <div class="relative bg-white dark:bg-gray-800 rounded-xl shadow-2xl w-full max-w-sm p-6 ring-1 ring-gray-200 dark:ring-gray-700">
        <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-2">Delete Account Type</h3>
        <p class="text-sm text-gray-500 dark:text-gray-400 mb-4">Are you sure you want to delete <strong data-account-type-masters-target="deleteModalName"></strong>? This cannot be undone.</p>
        <div data-account-type-masters-target="deleteModalError" class="hidden text-sm text-red-600 dark:text-red-400 mb-4"></div>
        <div class="flex justify-end space-x-3">
          <button type="button" class="px-4 py-2 text-sm font-medium text-gray-700 dark:text-gray-300 bg-gray-100 dark:bg-gray-700 hover:bg-gray-200 dark:hover:bg-gray-600 rounded-lg transition"
                  data-action="click->account-type-masters#closeDeleteModal">Cancel</button>
          <button type="button" class="px-4 py-2 text-sm font-medium text-white bg-red-600 hover:bg-red-700 rounded-lg transition"
                  data-action="click->account-type-masters#executeDelete">Delete</button>
        </div>
      </div>
    </div>
  </div>

  <%# Cannot-Delete Modal (in-use) %>
  <div class="hidden fixed inset-0 z-50 overflow-y-auto" data-account-type-masters-target="cannotDeleteModal">
    <div class="flex min-h-full items-center justify-center p-4">
      <div class="fixed inset-0 bg-black/50 transition-opacity" data-action="click->account-type-masters#closeCannotDeleteModal"></div>
      <div class="relative bg-white dark:bg-gray-800 rounded-xl shadow-2xl w-full max-w-md p-6 ring-1 ring-gray-200 dark:ring-gray-700">
        <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-2">Cannot Delete Account Type</h3>
        <div class="text-sm text-gray-500 dark:text-gray-400 mb-4" data-account-type-masters-target="cannotDeleteBody">
          <p>This account type is currently in use and cannot be deleted.</p>
        </div>
        <div class="flex justify-end">
          <button type="button" class="px-4 py-2 text-sm font-medium text-white bg-brand-600 hover:bg-brand-700 rounded-lg transition"
                  data-action="click->account-type-masters#closeCannotDeleteModal">OK</button>
        </div>
      </div>
    </div>
  </div>
</div>

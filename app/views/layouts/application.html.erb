<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>BudgetHQ</title>
    <%= csrf_meta_tags %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@tailwindcss/typography@0.5.10/dist/typography.min.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        darkMode: 'class',
        theme: {
          extend: {
            colors: {
              brand: {
                50:  '#faf5ff',
                100: '#f3e8ff',
                200: '#e9d5ff',
                300: '#d8b4fe',
                400: '#c084fc',
                500: '#a855f7',
                600: '#9333ea',
                700: '#7e22ce',
                800: '#6b21a8',
                900: '#581c87',
              }
            },
            fontFamily: {
              sans: ['Inter', 'system-ui', 'sans-serif'],
            }
          }
        }
      }
    </script>
    <script>
      // Apply saved theme immediately to prevent flash
      if (localStorage.getItem('theme') === 'dark' || (!localStorage.getItem('theme') && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
        document.documentElement.classList.add('dark')
      }
    </script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <%= javascript_importmap_tags %>
  </head>
  <body class="font-sans bg-gray-50 dark:bg-gray-900 min-h-screen transition-colors">
    <% if user_signed_in? %>
      <div data-controller="sidebar">
        <%= render "shared/navbar" %>
        <%= render "shared/flash" %>
        <main class="min-h-screen transition-all duration-300 md:ml-56 pt-14 md:pt-0" data-sidebar-target="mainContent">
          <%# Top header bar — h-14 aligns with sidebar logo border %>
          <div class="bg-white dark:bg-gray-800 border-b border-gray-200 dark:border-gray-700 px-4 sm:px-6 lg:px-8 h-14 flex items-center" data-controller="quotes">
            <div class="flex-1 flex items-center justify-between">
              <div class="flex items-center space-x-4 min-w-0">
                <h1 class="text-xl font-bold text-gray-900 dark:text-white whitespace-nowrap">Hello <%= current_user.first_name || current_user.full_name %></h1>
                <p class="text-base italic text-gray-400 dark:text-gray-500 truncate" data-quotes-target="text"></p>
              </div>
              <div class="flex items-center space-x-3 flex-shrink-0">
                <button type="button"
                        class="text-gray-400 dark:text-gray-500 hover:text-gray-600 dark:hover:text-gray-300 transition"
                        data-quotes-target="toggle"
                        data-action="click->quotes#toggle"
                        title="Toggle quotes">
                </button>
                <div class="flex items-center space-x-1.5 text-gray-500 dark:text-gray-400 text-sm">
                  <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/>
                  </svg>
                  <span><%= Date.today.strftime("%b %-d, %Y") %></span>
                </div>

                <div class="h-5 w-px bg-gray-300 dark:bg-gray-600"></div>

                <%# Theme toggle %>
                <div data-controller="theme">
                  <button type="button"
                          class="p-1.5 rounded-lg text-gray-500 dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-700 transition"
                          data-action="click->theme#toggle"
                          title="Toggle dark mode">
                    <svg data-theme-target="moonIcon" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                      <path stroke-linecap="round" stroke-linejoin="round" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z"/>
                    </svg>
                    <svg data-theme-target="sunIcon" class="h-5 w-5 hidden" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                      <path stroke-linecap="round" stroke-linejoin="round" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z"/>
                    </svg>
                  </button>
                </div>

                <%# User menu %>
                <div class="flex items-center space-x-2">
                  <div class="h-7 w-7 rounded-full bg-brand-500 flex items-center justify-center text-white text-xs font-bold flex-shrink-0">
                    <%= current_user.first_name&.first&.upcase || current_user.email.first.upcase %>
                  </div>
                  <span class="hidden sm:inline text-sm font-medium text-gray-700 dark:text-gray-300 truncate max-w-[8rem]"><%= current_user.first_name || current_user.email %></span>
                </div>

                <%# Settings %>
                <%= link_to edit_user_registration_path, class: "p-1.5 rounded-lg text-gray-500 dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-700 transition", title: "Settings" do %>
                  <svg class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.066 2.573c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.573 1.066c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.066-2.573c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"/>
                    <path stroke-linecap="round" stroke-linejoin="round" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/>
                  </svg>
                <% end %>

                <%# Sign out %>
                <%= button_to destroy_user_session_path, method: :delete, class: "p-1.5 rounded-lg text-gray-500 dark:text-gray-400 hover:bg-red-50 dark:hover:bg-red-900/20 hover:text-red-600 dark:hover:text-red-400 transition", title: "Sign out" do %>
                  <svg class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"/>
                  </svg>
                <% end %>
              </div>
            </div>
          </div>
          <%= yield %>
        </main>
      </div>
    <% else %>
      <%= render "shared/flash" %>
      <main>
        <%= yield %>
      </main>
    <% end %>
  </body>
</html>

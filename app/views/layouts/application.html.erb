<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>BudgetHQ</title>
    <%= csrf_meta_tags %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@tailwindcss/typography@0.5.10/dist/typography.min.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        darkMode: 'class',
        theme: {
          extend: {
            colors: {
              brand: {
                50:  '#faf5ff',
                100: '#f3e8ff',
                200: '#e9d5ff',
                300: '#d8b4fe',
                400: '#c084fc',
                500: '#a855f7',
                600: '#9333ea',
                700: '#7e22ce',
                800: '#6b21a8',
                900: '#581c87',
              }
            },
            fontFamily: {
              sans: ['Inter', 'system-ui', 'sans-serif'],
            }
          }
        }
      }
    </script>
    <script>
      // Apply saved theme immediately to prevent flash
      if (localStorage.getItem('theme') === 'dark' || (!localStorage.getItem('theme') && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
        document.documentElement.classList.add('dark')
      }
    </script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <%= javascript_importmap_tags %>
  </head>
  <body class="font-sans bg-gray-50 dark:bg-gray-900 min-h-screen transition-colors">
    <% if defined?(QA_MODE) && QA_MODE %>
      <div class="bg-red-600 text-white text-center py-4 px-4 font-bold text-lg tracking-wide z-[9999] sticky top-0">
        NEW RELEASE QA MODE &mdash; v<%= APP_VERSION %>
      </div>
    <% end %>
    <% if user_signed_in? %>
      <div data-controller="sidebar month-actions"
           data-month-actions-close-url-value="<%= close_api_open_month_master_path %>"
           data-month-actions-reopen-url-value="<%= reopen_api_open_month_master_path %>"
           data-month-actions-show-url-value="<%= api_open_month_master_path %>"
           data-month-actions-csrf-token-value="<%= form_authenticity_token %>">
        <%= render "shared/navbar" %>
        <%= render "shared/flash" %>
        <main class="min-h-screen flex flex-col transition-all duration-300 md:ml-56 pt-14 md:pt-0" data-sidebar-target="mainContent">
          <%# Top header bar — min-h-14 aligns with sidebar logo border, wraps on mobile %>
          <div class="sticky top-0 z-30 bg-white dark:bg-gray-800 border-b border-gray-200 dark:border-gray-700 px-4 sm:px-6 lg:px-8 min-h-[3.5rem] flex items-center py-2">
            <div class="flex-1 flex items-center justify-between min-w-0">
              <div class="flex items-center space-x-4 min-w-0 overflow-hidden flex-1 mr-4">
                <%# Sidebar toggle button (desktop/tablet) %>
                <button type="button"
                        class="hidden md:flex items-center justify-center w-8 h-8 rounded-lg text-gray-500 dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-700 transition flex-shrink-0"
                        data-action="click->sidebar#toggle"
                        title="Toggle sidebar">
                  <svg class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16"/>
                  </svg>
                </button>
                <div class="flex-shrink-0">
                  <h1 class="text-xl font-bold text-gray-900 dark:text-white whitespace-nowrap">Hello <%= current_user.first_name || current_user.full_name %></h1>
                  <% if @open_month_display.present? %>
                    <p class="text-xs font-bold text-gray-500 dark:text-gray-400"><%= @open_month_display %></p>
                  <% end %>
                </div>
                <p class="hidden sm:block text-sm italic text-gray-400 dark:text-gray-500 min-w-0" style="overflow-wrap: break-word; word-wrap: break-word;"><%= @daily_quote %></p>
              </div>
              <div class="flex items-center space-x-3 flex-shrink-0">
                <div class="flex items-center space-x-1.5 text-gray-500 dark:text-gray-400 text-sm">
                  <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/>
                  </svg>
                  <span><%= Date.today.strftime("%b %-d, %Y") %></span>
                </div>

                <div class="h-5 w-px bg-gray-300 dark:bg-gray-600"></div>

                <%# Profile dropdown (Theme, Settings, Logout) %>
                <div class="relative" data-controller="dropdown">
                  <button type="button"
                          class="flex items-center space-x-2 p-1 rounded-lg hover:bg-gray-100 dark:hover:bg-gray-700 transition"
                          data-action="click->dropdown#toggle">
                    <div class="h-7 w-7 rounded-full bg-brand-500 flex items-center justify-center text-white text-xs font-bold flex-shrink-0">
                      <%= current_user.first_name&.first&.upcase || current_user.email.first.upcase %>
                    </div>
                    <span class="hidden sm:inline text-sm font-medium text-gray-700 dark:text-gray-300 truncate max-w-[8rem]"><%= current_user.first_name || current_user.email %></span>
                    <svg class="h-4 w-4 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7"/></svg>
                  </button>
                  <div class="hidden absolute right-0 mt-2 w-48 rounded-lg bg-white dark:bg-gray-800 shadow-lg ring-1 ring-gray-200 dark:ring-gray-700 py-1 z-50"
                       data-dropdown-target="menu">
                    <%# Theme toggle %>
                    <div data-controller="theme" class="px-1">
                      <button type="button"
                              class="w-full flex items-center space-x-3 px-3 py-2 text-sm text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-700 rounded-md transition"
                              data-action="click->theme#toggle">
                        <svg data-theme-target="moonIcon" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                          <path stroke-linecap="round" stroke-linejoin="round" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z"/>
                        </svg>
                        <svg data-theme-target="sunIcon" class="h-4 w-4 hidden" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                          <path stroke-linecap="round" stroke-linejoin="round" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z"/>
                        </svg>
                        <span>Toggle Theme</span>
                      </button>
                    </div>
                    <%# Settings %>
                    <div class="px-1">
                      <%= link_to edit_user_registration_path, class: "flex items-center space-x-3 px-3 py-2 text-sm text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-700 rounded-md transition" do %>
                        <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                          <path stroke-linecap="round" stroke-linejoin="round" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.066 2.573c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.573 1.066c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.066-2.573c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"/>
                          <path stroke-linecap="round" stroke-linejoin="round" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/>
                        </svg>
                        <span>Settings</span>
                      <% end %>
                    </div>
                    <%# Soft Close Month %>
                    <div class="px-1">
                      <button type="button"
                              class="w-full flex items-center space-x-3 px-3 py-2 text-sm text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-700 rounded-md transition"
                              data-action="click->month-actions#confirmSoftClose">
                        <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                          <path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
                        </svg>
                        <span>Soft Close Month</span>
                      </button>
                    </div>
                    <%# Open Soft Close %>
                    <div class="px-1">
                      <button type="button"
                              class="w-full flex items-center space-x-3 px-3 py-2 text-sm text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-700 rounded-md transition"
                              data-action="click->month-actions#confirmOpenSoftClose">
                        <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                          <path stroke-linecap="round" stroke-linejoin="round" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"/>
                        </svg>
                        <span>Open Soft Close</span>
                      </button>
                    </div>
                    <%# Upgrade %>
                    <div class="px-1">
                      <%= link_to pricing_path, class: "flex items-center space-x-3 px-3 py-2 text-sm text-brand-600 dark:text-brand-400 hover:bg-brand-50 dark:hover:bg-brand-900/20 rounded-md transition" do %>
                        <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                          <path stroke-linecap="round" stroke-linejoin="round" d="M5 3l7 4 7-4v11a2 2 0 01-1.03 1.75L12 19l-5.97-3.25A2 2 0 015 14V3z"/>
                        </svg>
                        <span>Upgrade</span>
                      <% end %>
                    </div>
                    <div class="border-t border-gray-200 dark:border-gray-700 my-1"></div>
                    <%# Sign out %>
                    <div class="px-1">
                      <%= button_to destroy_user_session_path, method: :delete, class: "w-full flex items-center space-x-3 px-3 py-2 text-sm text-red-600 dark:text-red-400 hover:bg-red-50 dark:hover:bg-red-900/20 rounded-md transition" do %>
                        <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                          <path stroke-linecap="round" stroke-linejoin="round" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"/>
                        </svg>
                        <span>Sign Out</span>
                      <% end %>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
          <div class="flex-1">
            <%= yield %>
          </div>
          <%= render "shared/footer" %>
        </main>
      </div>
    <% else %>
      <%= render "shared/flash" %>
      <main class="min-h-screen flex flex-col">
        <div class="flex-1">
          <%= yield %>
        </div>
        <%= render "shared/footer" %>
      </main>
    <% end %>
    <% if user_signed_in? %>
      <%# Soft Close Confirmation Modal %>
      <div class="hidden fixed inset-0 z-[9999] flex items-center justify-center bg-black/50" data-month-actions-target="softCloseModal">
        <div class="bg-white dark:bg-gray-800 rounded-xl shadow-2xl max-w-md w-full mx-4 p-6">
          <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-3">Soft Close Month</h3>
          <p class="text-sm text-gray-600 dark:text-gray-400 mb-5" data-month-actions-target="softCloseMessage">Loading...</p>
          <div class="flex justify-end space-x-3">
            <button type="button" class="px-4 py-2 text-sm font-medium text-gray-700 dark:text-gray-300 bg-white dark:bg-gray-800 border border-gray-300 dark:border-gray-600 rounded-lg hover:bg-gray-50 dark:hover:bg-gray-700 transition"
                    data-action="click->month-actions#cancelSoftClose">Cancel</button>
            <button type="button" class="px-4 py-2 text-sm font-medium text-white bg-brand-600 hover:bg-brand-700 rounded-lg shadow-sm transition"
                    data-action="click->month-actions#executeSoftClose">Close Month</button>
          </div>
        </div>
      </div>
      <%# Open Soft Close Confirmation Modal %>
      <div class="hidden fixed inset-0 z-[9999] flex items-center justify-center bg-black/50" data-month-actions-target="openSoftCloseModal">
        <div class="bg-white dark:bg-gray-800 rounded-xl shadow-2xl max-w-md w-full mx-4 p-6">
          <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-3">Open Soft Close</h3>
          <p class="text-sm text-gray-600 dark:text-gray-400 mb-5" data-month-actions-target="openSoftCloseMessage">Loading...</p>
          <div class="flex justify-end space-x-3">
            <button type="button" class="px-4 py-2 text-sm font-medium text-gray-700 dark:text-gray-300 bg-white dark:bg-gray-800 border border-gray-300 dark:border-gray-600 rounded-lg hover:bg-gray-50 dark:hover:bg-gray-700 transition"
                    data-action="click->month-actions#cancelOpenSoftClose">Cancel</button>
            <button type="button" class="px-4 py-2 text-sm font-medium text-white bg-orange-600 hover:bg-orange-700 rounded-lg shadow-sm transition"
                    data-action="click->month-actions#executeOpenSoftClose">Re-open Month</button>
          </div>
        </div>
      </div>
      <%# Month Action Result Modal %>
      <div class="hidden fixed inset-0 z-[9999] flex items-center justify-center bg-black/50" data-month-actions-target="resultModal">
        <div class="bg-white dark:bg-gray-800 rounded-xl shadow-2xl max-w-md w-full mx-4 p-6">
          <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-3" data-month-actions-target="resultTitle">Result</h3>
          <p class="text-sm text-gray-600 dark:text-gray-400 mb-5" data-month-actions-target="resultMessage"></p>
          <div class="flex justify-end">
            <button type="button" class="px-4 py-2 text-sm font-medium text-white bg-brand-600 hover:bg-brand-700 rounded-lg shadow-sm transition"
                    data-action="click->month-actions#closeResult">OK</button>
          </div>
        </div>
      </div>
    <% end %>
    <%= render "shared/whats_new" if user_signed_in? %>
  </body>
</html>
